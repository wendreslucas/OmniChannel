import React from 'react';

import { StyledButton, TextButton } from './styles';

export const Button = () => {
  return (
    <StyledButton>
      <TextButton>Iniciar</TextButton>
    </StyledButton>
  );
};
