import styled from 'styled-components';

export const Container = styled.div`
  height: 48px;
  width: 48px;

  transition: ease-in-out 0.2s;
  &:hover {
    cursor: pointer;
    opacity: 0.8;
  }
`;
